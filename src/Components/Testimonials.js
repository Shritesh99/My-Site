import React, { Component } from 'react';

class Testimonials extends Component{
    render() {
        return(
            <div className="hero section is-warning"></div>
        );
    }
}

export default Testimonials;
